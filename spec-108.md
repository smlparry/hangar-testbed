# spec 108 — one committer

Scratch commit. Checking that a commit authored with the id-prefixed noreply
address renders as the hangar app on GitHub, with its avatar, rather than
resolving to whichever person happens to hold the login.
