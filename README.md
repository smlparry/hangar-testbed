# testbed

A scratch repo hangar's agents review against. Safe to delete.

`cart.js` holds the maths. `index.html` is a page that renders it, served by
`server.js` with no dependencies:

```
npm start        # http://localhost:8080
```
